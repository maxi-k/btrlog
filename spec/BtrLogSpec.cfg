\* SPECIFICATION
\* Uncomment the previous line and provide the specification name if it's declared
\* in the specification file. Comment INIT / NEXT parameters if you use SPECIFICATION.

CONSTANTS
    APPEND_COUNT_MAX = 4
    FAULTS_COUNT = 1 \* Controls the number of server faults to inject
    APPENDS_INFLIGHT_MAX = 2
    writer_ids = {w1, w2}
    reader_ids = {r1}
    server_ids = {s1,s2,s3}

SYMMETRY symmetry_set

\* INIT Init
\* NEXT Next
SPECIFICATION Spec

\*PROPERTY
\* Uncomment the previous line and add property names
    \* Liveness

INVARIANT
\* Uncomment the previous line and add invariant names
    type_check_ok
    entries_committed_readable
    no_reads_uncommitted

\* Enable the following to get statistics
\* _PERIODIC PrintStats \* Usable in simulation mode
\* POSTCONDITION PrintCoverage \* Useable in model checking and simulation mode
